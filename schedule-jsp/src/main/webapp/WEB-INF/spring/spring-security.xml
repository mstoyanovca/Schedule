<beans:beans 
	xmlns="http://www.springframework.org/schema/security"
	xmlns:beans="http://www.springframework.org/schema/beans" 
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
	
	http://www.springframework.org/schema/beans/spring-beans-4.1.xsd
	http://www.springframework.org/schema/security
	http://www.springframework.org/schema/security/spring-security-4.0.xsd">

	<http auto-config="true">
		<intercept-url pattern="/" method="GET" access="permitAll" />
		<intercept-url pattern="/login" method="GET" access="permitAll" />
		<!-- Activate a new account: -->
		<intercept-url pattern="/activate/{token}/" method="GET" access="permitAll" />
		<!-- Login with a password reset token: -->
		<intercept-url pattern="/reset-password/{token}/" method="GET" access="permitAll" />
		<!-- Check for taken email: -->
		<intercept-url pattern="/user/{email}/" method="HEAD" access="permitAll" />
		<!-- Request password reset: -->
		<intercept-url pattern="/user/{email}/" method="PUT" access="permitAll" />
		<!-- Register new account: -->
		<intercept-url pattern="/teacher" method="POST" access="permitAll" />

		<!-- Create session: -->
		<intercept-url pattern="/home" method="GET" access="hasRole('TEACHER')" requires-channel="https" />
		<!-- Protected resources: -->
		<intercept-url pattern="/schedule" method="GET" access="hasRole('TEACHER')" requires-channel="https" />
		<intercept-url pattern="/students" method="GET" access="hasRole('TEACHER')" requires-channel="https" />
		<!-- REST controller CRUD operations: -->
		<intercept-url pattern="/lesson" access="hasRole('TEACHER')" requires-channel="https" />
		<intercept-url pattern="/lesson/{lessonId}" access="hasRole('TEACHER')" requires-channel="https" />
		<intercept-url pattern="/student" access="hasRole('TEACHER')" requires-channel="https" />
		<intercept-url pattern="/student/{studentId}" access="hasRole('TEACHER')" requires-channel="https" />
		<intercept-url pattern="/account" access="hasRole('TEACHER')" requires-channel="https" />
		<intercept-url pattern="/teacher/{teacherId}" access="hasAnyRole('TEACHER', 'CHANGE_PASSWORD')" requires-channel="https" />
		<!-- Change password form: -->
		<intercept-url pattern="/change-password" access="hasAnyRole('TEACHER', 'CHANGE_PASSWORD')" requires-channel="https" />
		
		<form-login 
			login-page="/login" 
			username-parameter="email"
			password-parameter="password" 
			authentication-failure-url="/access_denied" />
			
		<custom-filter ref="csrfHeaderFilter" after="CSRF_FILTER"/>
		<csrf token-repository-ref="csrfTokenRepository" />
		
		<remember-me
        	remember-me-parameter="remember-me"
        	remember-me-cookie="remember-me"
        	token-validity-seconds="86400"
    		data-source-ref="dataSource" />
	</http>

	<authentication-manager>
		<authentication-provider user-service-ref="userDetailsService">
			<password-encoder ref="bcryptEncoder" />
		</authentication-provider>
	</authentication-manager>

	<beans:bean id="userDetailsService" class="com.schedule.security.UserDetailsServiceImpl" />
	<beans:bean id="bcryptEncoder" class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder" />
	<beans:bean id="csrfHeaderFilter" class="com.schedule.security.CsrfHeaderFilter" />
	<beans:bean id="csrfTokenRepository" class="org.springframework.security.web.csrf.HttpSessionCsrfTokenRepository">
    	<beans:property name="headerName" value="X-XSRF-TOKEN" />
    </beans:bean>
	
</beans:beans>
